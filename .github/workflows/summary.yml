name: Summary CI

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  summary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: upptime/summary@v1.0.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
